<!--  
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="semi.project.mapper.AssignmentMapper">
	
	<select id="aselectAll">
		select * from assignment 
	</select>
	
	<insert id="ainsert" parameterType="AssignmentVo">
		insert into assignment values(#{aseq}, #{rid}, #{thcode}, #{atitle},
		#{acontent}, SYSDATE, #{adeadline}, #{afname}, #{aofname}, #{afsize} )
	</insert>
</mapper>-->